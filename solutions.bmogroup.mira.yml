app-id: solutions.bmogroup.mira
runtime: org.kde.Platform
runtime-version: "6.10"
sdk: org.kde.Sdk
base: io.qt.PySide.BaseApp
base-version: "6.10"
command: mira

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri

build-options:
  env:
    PYSIDE_VERSION: "6.10.2"
    BASEAPP_REMOVE_WEBENGINE: "1"

modules:
  - pypi-dependencies.json
  - name: mira
    buildsystem: simple
    build-commands:
      - pip3 install --no-deps --prefix=${FLATPAK_DEST} mira_personal_finance_assistant-0.0.1rc2-py3-none-any.whl
      - install -Dm644 solutions.bmogroup.mira.desktop.in ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm644 solutions.bmogroup.mira.metainfo.xml.in ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm644 256x256.png ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
      - install -Dm644 scalable.svg ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/${FLATPAK_ID}.svg
    sources:
      - type: file
        url: https://mira.bmogroup.solutions/releases/mira_personal_finance_assistant-0.0.1rc2-py3-none-any.whl
        sha256: 6378e7d16b3870e9bd4746061671f7de3127c06563d1212b650f5c9ce4c324ff
      - type: file
        path: solutions.bmogroup.mira.desktop.in
      - type: file
        path: solutions.bmogroup.mira.metainfo.xml.in
      - type: file
        path: 256x256.png
      - type: file
        path: scalable.svg
cleanup:
  - "/include"
  - "/share/man"
